import logging

logger = logging.getLogger(__name__)

# TODO: implement in Phase 5
# search_people(query, user_id) -> List[PersonResponse]
# find_path(skill, my_person_id, user_id) -> List[PathResult]
