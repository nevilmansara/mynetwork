import logging

logger = logging.getLogger(__name__)

# TODO: implement in Phase 3
# get_all_people(user_id) -> List[PersonResponse]
# get_person(person_id, user_id) -> PersonResponse
# create_person(data, user_id) -> PersonResponse
# update_person(person_id, data, user_id) -> PersonResponse
# delete_person(person_id, user_id) -> None
# get_person_connections(person_id, user_id) -> List[PersonResponse]
