import logging

logger = logging.getLogger(__name__)

# TODO: implement in Phase 4
# get_all_connections(user_id) -> List[ConnectionResponse]
# create_connection(data, user_id) -> ConnectionResponse
# delete_connection(connection_id, user_id) -> None
# get_graph_data(user_id) -> { nodes, links }
