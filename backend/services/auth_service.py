import logging

logger = logging.getLogger(__name__)

# TODO: implement in Phase 2
# register_user(name, email, password) -> UserResponse
# login_user(email, password) -> token + user
# get_current_user(token) -> UserResponse
